pandas>=1.5.0
jsonschema>=4.0.0
openpyxl>=3.0.0  # SOLO lo usa validar_ia_vs_manual.py (pd.read_excel): herramienta manual sin entrada en CI

# ============================================================
# Motor Cualitativo IA (DeepSeek) — único motor desde v3.2.0
# ============================================================
# El motor legacy (spaCy + sentence-transformers) fue eliminado en v3.2.0.
# El cliente DeepSeek usa urllib (stdlib), NO requiere dependencias extra.
#
# DEEPSEEK_API_KEY es OBLIGATORIA para ejecutar el ETL.
# Se configura UNICAMENTE en GitHub Actions Secrets (Settings → Secrets and
# variables → Actions): el proyecto no se ejecuta en local, no hay .env.
#
# Variables opcionales del motor IA:
#   IA_CUALITATIVO_WORKERS=15    workers concurrentes
#   IA_CUALITATIVO_MAX_RPM=60    rate limit global
#   IA_CUALITATIVO_TIMEOUT=60    timeout por llamada (segundos)
#   IA_CUALITATIVO_MAX_FALLOS_API_PCT=20  umbral fail-closed de fallos de API
